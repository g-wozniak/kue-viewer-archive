export enum UIZones {
   App = 'app',
   ButtonSaveNodes = 'button-save-nodes',
   ButtonModalPathUpdate = 'button-modal-path-update',
   ButtonModalPathRollback = 'button-modal-path-rollback',
   ModalCardDetails = 'modal-card-details',
   ModalPath = 'modal-path',
   ModalMentor = 'modal-mentor'
}

export enum AsyncProcesses {
   Pull = 'proc-pull',
   Explore = 'proc-explore',
   Reset = 'proc-reset',
   SaveNodes = 'proc-save-nodes',
   Sync = 'proc-sync',
   SyncRollback = 'proc-sync-rollback'
}

export enum Modals {
   CardDetails = 'card-view-modal',
   Path = 'path-modal',
   Mentor = 'mentor-modal'
}